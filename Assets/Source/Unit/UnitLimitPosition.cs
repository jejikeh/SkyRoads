using UnityEngine;

namespace Source.Unit
{
    public class UnitLimitPosition : MonoBehaviour
    {
        /*
        [SerializeField] private Unit _unit;
        [SerializeField] private UnitMovable _unitMovable;

        private void Update()
        {
            if (transform.position.x > _unit.Config.XMaxPosition)
                transform.position = new Vector3(_unit.Config.XMaxPosition, transform.position.y, transform.position.z);
            
            if (transform.position.x < -_unit.Config.XMaxPosition)
                transform.position = new Vector3(-_unit.Config.XMaxPosition, transform.position.y, transform.position.z);
            
            if (transform.position.y > _unit.Config.YMaxPosition)
                transform.position = new Vector3(transform.position.x, _unit.Config.YMaxPosition, transform.position.z);

            if (transform.position.y < _unit.Config.YMinPosition)
                transform.position = new Vector3(transform.position.x, _unit.Config.YMinPosition, transform.position.z);
        }
        */
    }
}